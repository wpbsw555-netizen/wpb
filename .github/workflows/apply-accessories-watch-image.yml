name: Apply Accessories Watch Image
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Homepage now uses a static accessories image asset.'
